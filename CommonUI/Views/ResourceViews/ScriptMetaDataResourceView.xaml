<UserControl x:Class="CommonUI.Views.ResourceViews.ScriptMetadataResourceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.Column="0">Assembly</Label>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Path=Resource.AssemblyName}" IsReadOnly="True" />
        <Label Grid.Row="1" Grid.Column="0">ScriptSourceText</Label>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Path=Resource.ScriptSourceTextName}" IsReadOnly="True" />
        <Label Grid.Row="2" Grid.Column="0">Warnings</Label>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Path=Resource.Warnings}" IsReadOnly="True" Height="96" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
        <Label Grid.Row="3" Grid.Column="0">Properties</Label>
        <TreeView Grid.Row="3" Grid.Column="1" ItemsSource="{Binding Path=Resource.Properties}">
            <TreeView.ItemTemplate >
                <HierarchicalDataTemplate ItemsSource="{Binding Path=Resource.Properties}">
                    <TextBlock Text="asdf" />
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
        <Label Grid.Row="4" Grid.Column="0">Strings</Label>
        <DataGrid Grid.Row="4" Grid.Column="1" ItemsSource="{Binding Path=Resource.Strings, Mode=OneWay}"  IsReadOnly="True" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Key}" ClipboardContentBinding="{x:Null}" Header="Key" Width="*" />
                <DataGridTextColumn Binding="{Binding Value}" ClipboardContentBinding="{x:Null}" Header="Value" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
